sudo: required
services:
  - docker

before_install:
  - docker build -t react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run react-test npm test -- --coverage

after_success:
  - docker build multi-client ./client
  - docker build multi-server ./server
  - docker build multi-nginx ./nginx
  - docker build multi-worker ./worker

  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password=stdin

  - docker push multi-client
  - docker push multi-server
  - docker push multi-nginx
  - docker push multi-worker
